package Scheduler::Web::C::Test;

use strict;
use warnings;
use utf8;
use Time::Piece;
use DDP;
use JSON;

sub test1 {
    my ( $class, $c ) = @_;

    return $c->render('ajax/ajax_test1.tx');

}

1;

